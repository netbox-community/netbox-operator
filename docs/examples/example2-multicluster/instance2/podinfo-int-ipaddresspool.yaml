apiVersion: kro.run/v1alpha1
kind: MetalLBIPAddressPoolFromNetBoxParentPrefixSelector
metadata:
  name: podinfo2
spec:
  name: podinfo2
  tenant: "MY_TENANT" # Use the `name` value instead of the `slug` value
  site: "DM-Akron" # Use the `name` value instead of the `slug` value
  description: "instance2"
  comments: "podinfo in pss namespace"
  preserveInNetbox: true
  prefixLength: "/32"
  parentPrefixSelector:
    environment: prod
