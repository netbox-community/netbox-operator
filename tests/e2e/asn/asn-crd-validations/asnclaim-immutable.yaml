---
apiVersion: netbox.dev/v1
kind: AsnClaim
metadata:
  name: asn-crd-validations-claim-immutable
spec:
  # non existing ASN Range on purpose, this claim must never reserve an ASN
  parentAsnRange: "NON_EXISTING_ASN_RANGE"
  tenant: "MY_TENANT"
