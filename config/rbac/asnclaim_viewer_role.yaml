# permissions for end users to view asnclaims.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: asnclaim-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: netbox-operator
    app.kubernetes.io/part-of: netbox-operator
    app.kubernetes.io/managed-by: kustomize
  name: asnclaim-viewer-role
rules:
- apiGroups:
  - netbox.dev
  resources:
  - asnclaims
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - netbox.dev
  resources:
  - asnclaims/status
  verbs:
  - get
